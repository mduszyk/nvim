local plugins = {}

table.insert(plugins, require("plugins.theme"))
table.insert(plugins, require("plugins.neotree"))
table.insert(plugins, require("plugins.cmp"))
table.insert(plugins, require("plugins.plugins"))

return plugins
